# IMS Hirosaki Portal — プロジェクト規約

日本の組織向け社内業務グループウェア。WordPress プラグイン（モジュラーモノリス）。
**応答・コメント・コミットメッセージはすべて日本語で書くこと。**

## 正本の優先順位

1. `docs/` の要件定義書（`00`〜`08`）が**唯一の正本**。コードと食い違う場合は要件定義書が正しい。
2. 実装前に、該当モジュールの要件定義書の**該当章を必ず読む**。読まずに書き始めない。
3. `docs/引き継ぎ書_phase2b.md` に現在地・確定事項・保留理由がある。作業開始時に読む。
4. デザインは `docs/07_design_system.md`（和紙・墨）が正本。
   `00_portal.md` 第6章の青系トークンは**古い記述なので無視する**。

## 環境

- PHP 8.3 / WordPress。Composer はサーバーに無い（**軽量PSR-4オートローダーを自前実装している**）。
- `main` への push で GitHub Actions が自動デプロイ。**`.github/` は絶対に触らない。**
- テスト環境：`portal-site.labs-ims.com`

## アーキテクチャの決まりごと

- 業務ロジックは**すべてプラグイン側**。テーマには置かない。
- トランザクションデータは**独自テーブル**。カスタム投稿タイプは使わない。
- コアはモジュールに依存しない。モジュールが**フックで自己登録**する：
  `ims_portal_register_page` / `ims_portal_register_tile` /
  `ims_portal_summary_contribute` / `ims_register_schema` / `ims_portal_dashboard_top`

### 実装済みコードの作法（要件定義書 08 の記述と食い違う。**実装側に合わせること**）

- `Router::register()` は**スラッグ文字列のみ**（例：`'timecard'`）。
  フックに渡ってくるのは**インスタンスではなくクラス名の文字列**。
- `TileRegistry::add()` の `caps` は**capability 名**（`ims_use_portal` 等）。ロール名ではない。
- コアの `Assets` は**モジュール固有CSS/JSを積まない**。各モジュールが自前で enqueue する。
- `ims-portal.php` の `boot()` が各モジュールの `Bootstrap::init()` を直接呼ぶ。
  これがコア無改修原則の唯一の例外。新モジュール追加時はここに1行足す。

## 実装の作法

- **1機能を細かくスライスして進める。** 特に書き込み・外部送信・メール送信を伴う処理は単独スライスにする。
- **既存実装は依頼された箇所だけ変更する。** 動いている部分に手を入れない。
- 共有CSS（`admin.css` / `portal.css`）は**直接変更しない**。
  画面スコープ付きクラス（`.ims-tc-settings` / `.tc-wrap` 等）で新規CSSファイルを作る。
- 判定ロジックは**純粋関数として切り出す**（例：`StatusCalculator`）。DB/WPに触れずテストできる形にする。
  同じ判定を UI 側と API 側で二重実装しない。
- 管理画面の文言は**非技術者向けの平易な日本語**。開発者用語を使わない。
  （例：「Webhook URL」ではなく「通知先のチャットルーム URL」）
- 認証情報（OAuthシークレット、Webhook URL 等）は `Crypto` で**暗号化して保存**。Git に平文で置かない。
- 外部送信は**ベストエフォート**。失敗しても業務処理をブロックしない。
  リトライは `sleep()` せず WP-Cron の単発イベントで行う。

## DB の作法

- DDL は各モジュールの `Schema.php` が正本。`ims_register_schema` フィルタで寄与する。
- dbDelta 互換：`PRIMARY KEY` の後は**半角スペース2つ**、`INDEX` ではなく `KEY`、型は小文字、1カラム1行。
- **スキーマを変更したら `ims-portal.php` の `IMS_PORTAL_DB_VERSION` を必ずインクリメントする。**
- 打刻ログ等の監査対象データは**物理削除しない**。
- SQL は必ず `$wpdb->prepare` を通す。ユーザーデータは必ず `user_id` で絞る。

## 完了前に必ずやること

1. 変更した全PHPファイルに `php -l` を通す。
2. ロジックを足したらスモークテストを書いて通す（WordPress 無しで動く形）。
3. 要件定義書の該当章と照合し、**仕様から意図的に外した点があれば理由とともに報告する**。
4. `ims-portal.php` のバージョンを更新する（`0.5.1-phase2b` 形式）。

## コミット

- 形式：`feat(モジュール名): 日本語の説明 (Phase 2x)`
- 例：`feat(timecard): 打刻APIを追加 (Phase 2c)`
- 1スライス＝1コミット。複数スライスをまとめない。
- **push すると即デプロイされる。** DB_VERSION を上げた場合は、push 後に
  wp-admin を開いて `ims_db_version` が上がったか確認するよう利用者に伝える。
- 新規URL（ポータル画面）を追加した場合は、パーマリンク再保存が必要だと伝える。

## 保留中で、勝手に実装してはいけないもの

- **残業乖離アラートの検知本体** … 比較対象 `overtime_end_time` が 05_稟議モジュール側。未作成。
- **月次締め後のロック** … 判定対象 `wp_monthly_summary` が 03_勤怠モジュール側。未作成。

いずれも「テーブル不在時は無効」として1メソッドに閉じ込め、該当モジュール実装時に接続する。